# Core
pandas==2.2.2
numpy==1.26.4

# ML for fraud detection (rule-based primary, IsolationForest optional/future)
scikit-learn==1.5.1

# Database
# sqlite3 is built into Python's standard library — no pip install needed

# Dashboard (Day 3)
streamlit==1.37.1

# LLM chat/voice layer (Day 2)
groq>=0.11.0
gTTS==2.5.1

# Utilities
python-dotenv==1.0.1
faker==26.0.0
